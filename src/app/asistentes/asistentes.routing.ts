import { Routes } from '@angular/router';

import { AsistentesComponent } from './asistentes.component';

export const AsistentesRoutes: Routes = [
    {
        path: '',
        component: AsistentesComponent
    }
];
